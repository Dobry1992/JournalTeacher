@model Portal.Models.Subject

@{
    ViewData["Title"] = "Предмет";
}

@await Html.PartialAsync("_NavMenuJournal")

<div style="padding:10%">
    <section class="content-header">
        <div class="container-fluid">
        </div>
    </section>
    <section class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-body">
                            <h6 class="mb-3 text-center bg-warning headGroup p-2 rounded">Кафедра: @Model.Department.Name,<br />предмет: @Model.Name</h6>
                            <div id="example1_wrapper" class="dataTables_wrapper dt-bootstrap4">
                                <div class="row">
                                    <div class="col-sm-12 table-wrap">
                                        <a asp-controller="Themes" asp-action="CreateTheme" asp-route-id="@Model.SubjectID" class="btn btn-primary mb-3">Добавить тему</a>
                                        <table id="table_themes" class="table table-striped dataTable dtr-inline compact" role="grid">
                                            <thead class="thead-light">
                                                <tr role="row">
                                                    <th class="sorting">Номер, вид занятия, тема в соответствии С УТП</th>
                                                    <th class="sorting">Номер темы</th>
                                                    <th class="sorting">Действие</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                @foreach (var item in Model.Themes)
                                                {
                                                    @if (item.Arch == false && item.Name != "Контрольное занятие")
                                                    {
                                                        <tr role="row" class="odd bg-white">
                                                            <td tabindex="0" class="sorting_1">@Html.DisplayFor(modelItem => item.Name)</td>
                                                            <td tabindex="0" class="sorting_1">@item.ShortName</td>
                                                            <td tabindex="0" class="sorting_1">
                                                                <div class="container">
                                                                    <a title="Редактировать" class="btn btn-warning" asp-controller="Themes" asp-action="EditTheme" asp-route-id="@item.ThemeID" asp-route-SubjectID="@Model.SubjectID"><i class="fa fa-file-edit"></i></a>
                                                                </div>
                                                            </td>
                                                        </tr>
                                                    }
                                                    else if (item.Name != "Контрольное занятие")
                                                    {
                                                        <tr role="row" class="odd bg-gray">
                                                            <td tabindex="0" class="sorting_1">@Html.DisplayFor(modelItem => item.Name)</td>
                                                            <td tabindex="0" class="sorting_1">@item.ShortName</td>
                                                            <td tabindex="0" class="sorting_1">
                                                                <div class="container">
                                                                    <a title="Редактировать" class="btn btn-warning" asp-controller="Themes" asp-action="Edit" asp-route-id="@item.ThemeID" asp-route-SubjectID="@Model.SubjectID"><i class="fa fa-file-edit"></i></a>
                                                                </div>
                                                            </td>
                                                        </tr>
                                                    }
                                                }
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>

@section scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}