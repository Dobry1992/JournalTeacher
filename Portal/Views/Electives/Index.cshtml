@model IEnumerable<Portal.Models.Elective.Elective>

@{
    ViewData["Title"] = "Факультативы";
    int i = 1;
}

@await Html.PartialAsync("_NavMenuJournal")

<div>
    <section class="content-header">
        <div class="container-fluid">
            <div class="row">
                <div class="col-sm-6">
                    <h3 class="mb-1">Факультативы</h3>
                </div>
            </div>
        </div>
    </section>
    <section class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-body">
                            <div id="example1_wrapper" class="dataTables_wrapper dt-bootstrap4">
                                <div class="row">
                                    <div class="col-sm-12 table-wrap">
                                        <a asp-action="ElectiveCreate" class="btn btn-primary mb-3">Добавить факультатив</a>
                                        <table id="Table" class="table table-bordered dataTable dtr-inline table-sm compact" role="grid">
                                            <thead class="thead-dark">
                                                <tr role="row">
                                                    <th class="sorting_asc">#</th>
                                                    <th class="sorting">Факультатив</th>
                                                    <th class="sorting">Кафедра</th>
                                                    <th class="sorting fix">Действие</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                @foreach (var item in Model)
                                                {
                                                    <tr role="row" class="odd bg-white">
                                                        <td tabindex="0" class="sorting_1">@i</td>
                                                        <td tabindex="0" class="sorting_1">@Html.DisplayFor(modelItem => item.Name)</td>
                                                        @foreach (var department in ViewBag.Departments)
                                                        {
                                                            if (department.DepartmentID == item.DepartmentID)
                                                            {
                                                                <td tabindex="0" class="sorting_1">@department.Name</td>
                                                            }
                                                        }
                                                        <td tabindex="0" class="sorting_1 fix">
                                                            <div class="container">
                                                                <a title="Редактировать" class="btn btn-warning btn-sm" asp-controller="Electives" asp-action="Edit" asp-route-id="@item.ElectiveID"><i class="fa fa-file-edit"></i></a>
                                                                <a title="Информация" class="btn btn-info btn-sm" asp-controller="ElectiveThemes" asp-action="Index" asp-route-id="@item.ElectiveID"><i class="fa fa-info"></i></a>
                                                                <a title="Удалить" class="btn btn-danger btn-sm" asp-controller="Electives" asp-action="Delete" asp-route-id="@item.ElectiveID"><i class="fa fa-remove"></i></a>
                                                            </div>
                                                        </td>
                                                    </tr>
                                                    i++;
                                                }
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>

@section scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
