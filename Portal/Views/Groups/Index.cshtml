@model IEnumerable<Portal.Models.Group>

@{
    ViewData["Title"] = "Учебные группы";
    int i = 1;
}

@await Html.PartialAsync("_NavMenuJournal")

<div>
    <section class="content-header">
        <div class="container-fluid">
            <div class="row">
                <div class="col-sm-6">
                    <h3 class="mb-1">Учебные группы</h3>
                </div>
            </div>
        </div>
    </section>
    <section class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-body">
                            <div id="example1_wrapper" class="dataTables_wrapper dt-bootstrap4">
                                <div class="row">
                                    <div class="col-sm-12 table-wrap">
                                        <a asp-action="Create" class="btn btn-primary mb-3">Добавить группу</a>
                                        <table id="Table" class="table table-bordered table-sm compact dataTable dtr-inline" role="grid">
                                            <thead class="thead-dark">
                                                <tr role="row">
                                                    <th class="sorting_asc">#</th>
                                                    <th class="sorting">Группа</th>
                                                    <th class="sorting">Специальность</th>
                                                    <th class="sorting">Даты обучения</th>
                                                    <th class="sorting fix">Действие</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                @foreach (var item in Model)
                                                {
                                                    @if (item.DateExit > DateTime.Now)
                                                    {
                                                        <tr role="row" class="odd bg-white">
                                                            <td tabindex="0" class="sorting_1">@i</td>
                                                            <td tabindex="0" class="sorting_1">@Html.DisplayFor(modelItem => item.Name)</td>
                                                            <td tabindex="0" class="sorting_1">@Html.DisplayFor(modelItem => item.Speciality.Name)</td>
                                                            <td tabindex="0" class="sorting_1">@item.DateEnter.ToShortDateString() - @item.DateExit.ToShortDateString()</td>
                                                            <td tabindex="0" class="sorting_1 fix">
                                                                <div class="container">
                                                                    <a title="Редактировать" class="btn btn-warning btn-sm" asp-controller="Groups" asp-action="Edit" asp-route-id="@item.GroupID"><i class="fa fa-file-edit"></i></a>
                                                                    <a title="Информация" class="btn btn-info btn-sm" asp-controller="Groups" asp-action="Details" asp-route-id="@item.GroupID"><i class="fa fa-info"></i></a>
                                                                    <a title="Добавить слушателя/курсанта" class="btn btn-primary btn-sm" asp-controller="Students" asp-action="Create" asp-route-id="@item.GroupID"><i class="fa fa-people-group"></i></a>
                                                                    <a title="Удалить" class="btn btn-danger btn-sm" asp-controller="Groups" asp-action="Delete" asp-route-id="@item.GroupID"><i class="fa fa-remove"></i></a>
                                                                </div>
                                                            </td>
                                                        </tr>
                                                    }
                                                    else
                                                    {
                                                        <tr role="row" class="odd bg-gray">
                                                            <td tabindex="0" class="sorting_1">@i</td>
                                                            <td tabindex="0" class="sorting_1">@Html.DisplayFor(modelItem => item.Name)</td>
                                                            <td tabindex="0" class="sorting_1">@Html.DisplayFor(modelItem => item.Speciality.Name)</td>
                                                            <td tabindex="0" class="sorting_1">@item.DateEnter.ToShortDateString() - @item.DateExit.ToShortDateString()</td>
                                                            <td tabindex="0" class="sorting_1 fix">
                                                                <div class="container">
                                                                    <a title="Редактировать" class="btn btn-warning btn-sm" asp-controller="Groups" asp-action="Edit" asp-route-id="@item.GroupID"><i class="fa fa-file-edit"></i></a>
                                                                    <a title="Информация" class="btn btn-info btn-sm" asp-controller="Groups" asp-action="Details" asp-route-id="@item.GroupID"><i class="fa fa-info"></i></a>
                                                                    <a title="Добавить слушателя/курсанта" class="btn btn-primary btn-sm" asp-controller="Students" asp-action="Create" asp-route-id="@item.GroupID"><i class="fa fa-people-group"></i></a>
                                                                    <a title="Удалить" class="btn btn-danger btn-sm" asp-controller="Groups" asp-action="Delete" asp-route-id="@item.GroupID"><i class="fa fa-remove"></i></a>
                                                                </div>
                                                            </td>
                                                        </tr>
                                                    }
                                                    i++;
                                                }
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>

@section scripts{
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
